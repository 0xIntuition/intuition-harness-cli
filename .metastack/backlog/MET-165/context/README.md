# Context Index: Technical: Implement the end-to-end `meta merge` command, dashboard, agent merge-run orchestration, and aggregate PR publication

Last updated: 2026-03-16

Use this folder for repository-local research and baseline evidence for the `meta merge` implementation.

## Current Notes

- [`./context-note-template.md`](./context-note-template.md): current CLI, dashboard, workspace, and agent-orchestration baseline derived from local repo files.

## Authoring Rules

1. Prefer repository evidence first: `README.md`, `src/*.rs`, `.metastack/codebase/*.md`, and existing tests.
2. When external references become necessary, record the exact source and capture date.
3. End each note with explicit implications for this backlog item so the note can be acted on during implementation.
