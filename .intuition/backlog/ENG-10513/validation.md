# Validation Plan

## Command Proofs

- Run the changed CLI flow against a deterministic local or mocked setup
- Verify the original Linear issue description for `ENG-10513` remains unchanged
- Update the existing `## Codex Workpad` comment with validation notes instead of running `intu sync push`

## Notes

- `intu listen` must not overwrite the primary Linear issue description.
