# Listen Verification Inputs

Place deterministic battle-test inputs for `agents.listen` in this directory.
Verification samples files in sorted order up to the install-scoped
`battle_test_count` limit and ignores this README plus hidden placeholder files.
