# ENG-10746 Validation Evidence

## Summary

Repo-level reviewer evidence for ENG-10746: listen-store JSON persistence now
uses same-directory temp files plus atomic persist semantics, startup-critical
listen metadata recovers deterministically from sibling artifacts when the
primary JSON is corrupt, and active-listener lock acquisition and cleanup now
distinguish corrupt/stale orphaned locks from valid live locks without deleting
replacement lock files.

## Command Proofs

- `cargo test listen::store --lib`
- `cargo test --test listen -- --test-threads=1`
- `cargo test session_runtime::tests::write_json_keeps_existing_file_readable_when_tempfile_creation_fails --lib -- --exact`
- `cargo test listen::store::tests::acquire_listener_lock_removes_corrupt_orphaned_lock --lib -- --exact`
- `cargo test listen::store::tests::acquire_listener_lock_recovers_live_lock_from_backup_before_blocking --lib -- --exact`
- `cargo test --test listen listen_recovers_corrupt_session_json_from_backup -- --exact`
- `cargo test --test listen listen_rejects_duplicate_listener_when_corrupt_primary_lock_recovers_live_backup -- --exact`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `make quality`

## Results

- `cargo test listen::store --lib`
  - Passed with `28 passed; 0 failed`.
  - Proved required-state recovery ordering, project metadata recovery, corrupt
    orphaned lock cleanup, recovered live-lock blocking, and replacement-lock
    safety in the store layer.
- `cargo test --test listen -- --test-threads=1`
  - Passed with `92 passed; 0 failed`.
  - Exercised the full CLI listen matrix, including corrupt `session.json`
    recovery and duplicate-listener rejection from recovered live locks.
- `cargo test session_runtime::tests::write_json_keeps_existing_file_readable_when_tempfile_creation_fails --lib -- --exact`
  - Passed with `1 passed; 0 failed`.
  - Seeded proof that a temp-file creation failure leaves the previous primary
    JSON readable instead of truncating it in place.
- `cargo test listen::store::tests::acquire_listener_lock_removes_corrupt_orphaned_lock --lib -- --exact`
  - Passed with `1 passed; 0 failed`.
  - Seeded proof that corrupt or unreadable orphaned
    `active-listener.lock.json` files are warned and removed.
- `cargo test listen::store::tests::acquire_listener_lock_recovers_live_lock_from_backup_before_blocking --lib -- --exact`
  - Passed with `1 passed; 0 failed`.
  - Seeded proof that a corrupt primary lock recovers from a valid `.bak`
    sibling and still blocks duplicate listeners for a live PID.
- `cargo test --test listen listen_recovers_corrupt_session_json_from_backup -- --exact`
  - Passed with `1 passed; 0 failed`.
  - Seeded CLI proof that corrupt `session.json` is restored from a valid
    sibling recovery artifact and rewritten into the primary path.
- `cargo test --test listen listen_rejects_duplicate_listener_when_corrupt_primary_lock_recovers_live_backup -- --exact`
  - Passed with `1 passed; 0 failed`.
  - Seeded CLI proof that a corrupt primary lock recovers from backup and
    preserves the existing duplicate-listener failure behavior for a live
    listener.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.
- `make quality`
  - Passed.
  - Covered `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`,
    the full `cargo test -- --test-threads=1` suite, and doc tests.

## Acceptance Criteria Mapping

- `src/session_runtime.rs` now writes JSON through same-directory temp files,
  `sync_all()`, and atomic `persist` / `persist_noclobber`, preserving overwrite
  and exclusive-create semantics without exposing partially written final files.
- `src/listen/store.rs` now applies deterministic required-file recovery to
  `project.json`, `session.json`, and `active-listener.lock.json`, preferring
  `.bak` before `.tmp`, logging successful recovery, and rewriting the primary
  path atomically.
- `src/listen/store.rs` keeps optional detail artifacts best-effort while
  returning clear unrecoverable errors for startup-critical metadata when no
  valid recovery snapshot exists.
- `src/listen/store.rs` now removes stale or corrupt orphaned locks, preserves
  blocking behavior for valid live locks, and uses file identity in
  `ListenerLockGuard::drop` so replacement locks are not deleted accidentally.
- `src/session_runtime.rs`, `src/listen/store.rs`, and `tests/listen.rs` now
  cover torn-write preservation, required recovery success and failure, lock
  cleanup, live-lock blocking, and replacement-lock safety.
- `README.md`, `docs/agent-daemon.md`, `docs/listen-phased-execution-spec.md`,
  and `WORKFLOW.md` document the hardened persistence and recovery contract
  without changing schemas or CLI surface area.

## Notes

- The first continuation-turn `make quality` rerun failed in `tests/workspace.rs`
  because the workspace had only about 1.7 GiB free and generated
  `target/debug/incremental` artifacts consumed about 2.1 GiB. Removing that
  generated directory inside the workspace restored headroom, `cargo test --test workspace -- --test-threads=1`
  passed, and the full `make quality` rerun then passed cleanly.
- The continuation-turn timeout root cause was operational rather than a wedged
  Cargo lock: no orphaned `cargo` or `make quality` processes remained from the
  previous timed-out turn.
- The primary Linear issue description was not modified during this work.
  Progress and validation were kept in the existing `## Codex Workpad` comment
  and the local ticket artifacts.
