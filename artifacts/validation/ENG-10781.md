# ENG-10781 Validation Evidence

## Summary

Repo-level reviewer evidence for ENG-10781: `backlog improve` now uses one
shared terminal-session lifecycle across the interactive dashboard, review, and
loading transitions so terminal state restoration happens before the command
prints its final shell-facing summary; the improvement prompt no longer seeds a
technical label example for plan-oriented tickets; and improve-label
normalization now preserves issue-provided labels while blocking new `tech` or
`technical` injection attributable to the improve flow.

The continuation follow-up also closed the only remaining handoff risk from the
previous listen turn: the long-running `make quality` gate that contributed to
the timeout exposure was completed successfully in this turn, with no remaining
background validation work left open before review handoff.

## Command Proofs

- `cargo test --test backlog_improve -- --test-threads=1`
- `cargo test render_improvement_prompt_does_not_seed_technical_labels -- --exact`
- `cargo test normalize_improvement_output_does_not_introduce_technical_labels_for_plan_tickets -- --exact`
- `cargo test backlog_improve_apply_preserves_existing_labels_without_injecting_technical -- --exact`
- `cargo test backlog_improve_interactive_cleanup_restores_terminal_state_before_return -- --exact`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `make quality`

## Results

- `cargo test --test backlog_improve -- --test-threads=1`
  - Passed.
  - Covered the repo-scoped `backlog improve` integration surface after the
    terminal-lifecycle and label-normalization changes.
- `cargo test render_improvement_prompt_does_not_seed_technical_labels -- --exact`
  - Passed.
  - Proved the rendered improvement prompt no longer includes a seeded
    `technical` label in the example JSON payload.
- `cargo test normalize_improvement_output_does_not_introduce_technical_labels_for_plan_tickets -- --exact`
  - Passed.
  - Proved normalized improve output drops newly introduced `tech` and
    `technical` labels for plan-oriented tickets while preserving the intended
    labels from the proposal.
- `cargo test backlog_improve_apply_preserves_existing_labels_without_injecting_technical -- --exact`
  - Passed.
  - Proved apply payload construction preserves existing issue labels and does
    not originate a new technical label when the improve output attempts to add
    one.
- `cargo test backlog_improve_interactive_cleanup_restores_terminal_state_before_return -- --exact`
  - Passed.
  - Provided a deterministic shell-facing cleanup proof via
    `backlog_improve_terminal_cleanup_bytes()`, confirming the `backlog improve`
    exit bytes restore cursor visibility, mouse capture, bracketed paste, raw
    mode, and alternate-screen state before any trailing summary output is
    emitted back to the parent shell.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.
- `make quality`
  - Passed cleanly.
  - Re-ran `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`,
    the full single-threaded test suite, and doc tests under the repository's
    primary quality gate.

## Acceptance Criteria Mapping

- `src/backlog_improve.rs` now owns one symmetric terminal-session contract for
  `backlog improve` interactive flows, covering the dashboard, review, and
  loading transitions without leaving split cleanup guards behind.
- `src/backlog_improve.rs` restores the terminal session before returning the
  final summary string on success, and the same lifecycle now covers shared
  early-exit and cancel paths that use the interactive session.
- `src/backlog_improve.rs` also removes the prompt-level technical-label bias
  and normalizes improve labels against the issue's existing label set so the
  improve flow preserves legitimate pre-existing labels without introducing a
  new `tech` or `technical` label.
- `tests/backlog_improve.rs` adds targeted regressions for prompt rendering,
  normalized labels, apply payload behavior, and deterministic interactive
  cleanup ordering.
- Existing improve artifact generation and local-before-remote apply ordering
  were preserved; the changes stayed scoped to `backlog improve` plus one
  compile-fix in `src/listen/store.rs` required after syncing the branch with
  `origin/main`.

## Notes

- The branch was refreshed against `origin/main` before validation; the
  resulting workspace base was commit `a7cc021`.
- The reviewer-facing deterministic cleanup proof intentionally avoids a flaky
  PTY dependency by asserting the emitted cleanup bytes directly through a
  hidden test helper re-exported from `src/lib.rs`.
- The primary Linear issue description was not overwritten during this run; the
  existing Codex workpad comment was used for progress and validation updates
  instead.

## Handoff Status

- The implementation and validation proofs above were completed on commit
  `de94915`.
- Later docs-only checkpoint commits `ab707ed`, `ece9b78`, and `a28e0d9`
  reconciled review and handoff metadata after the code landed; those commits
  did not change runtime behavior.
- At the `2026-04-09T16:06:34Z` checkpoint snapshot, draft PR `#104` targeted
  `main` and pointed at branch head `a28e0d9`.
- At that same snapshot, GitHub Actions had one exact-head `quality` run in
  progress for `a28e0d9` (run `24200292333`, started `2026-04-09T16:03:59Z`),
  so this checkpoint turn did not attempt a ready-for-review promotion.
- The draft PR body was refreshed to remove stale heuristic placeholders while
  preserving the `metastack` label on the PR.
- No GitHub PR comments were present beyond the automatic Linear linkback at
  the time of that snapshot.
