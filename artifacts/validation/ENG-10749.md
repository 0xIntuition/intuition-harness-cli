# ENG-10749 Validation Evidence

## Summary

Repo-level reviewer evidence for ENG-10749: listen session merging now rejects
PR #95-style stale daemon overwrites of protected worker-owned state, structured
verification requires a passing `quality` workflow for the exact active PR HEAD
SHA, and structured review prompts explicitly call out cross-actor state
ownership plus the highest-risk listen modules. Review-handoff flow was also
hardened so final verification resolves or creates the draft branch PR before
applying the exact-SHA verification gate.

## Command Proofs

- `cargo test listen_review_prompt_calls_out_cross_actor_state_ownership_risks -- --exact`
- `cargo test listen_state_merge_rejects_stale_daemon_overwrite_of_enriched_worker_session -- --exact`
- `cargo test listen_verification_requires_quality_workflow_success_for_exact_pr_head_sha -- --exact`
- `cargo test --test listen -- --test-threads=1`
- `cargo test --test listen listen_worker_times_out_builtin_turns_and_records_timeout_reporting -- --exact`
- `cargo test --test listen listen_worker_logs_final_sigkill_termination_for_stubborn_builtin_timeouts -- --exact`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `make quality`

## Results

- `cargo test listen_review_prompt_calls_out_cross_actor_state_ownership_risks -- --exact`
  - Passed.
  - Proved the prompt templates now explicitly call out cross-actor ownership
    review heuristics and the high-risk listen module list.
- `cargo test listen_state_merge_rejects_stale_daemon_overwrite_of_enriched_worker_session -- --exact`
  - Passed.
  - Proved stale daemon snapshots cannot overwrite richer worker-owned blocked
    metadata, canonical metadata, `turn_history`, `latest_resume_handle`, or the
    accumulated turn count.
- `cargo test listen_verification_requires_quality_workflow_success_for_exact_pr_head_sha -- --exact`
  - Passed.
  - Proved verification fails closed for missing PRs, stale green runs from older
    commits, pending runs, failed runs, missing workflow results, and GitHub
    lookup failures unless the exact active PR HEAD SHA has a passing `quality`
    workflow.
- `cargo test --test listen -- --test-threads=1`
  - Passed with `84 passed; 0 failed`.
  - Exercised the full listen integration matrix, including PR handoff,
    verification remediation, exact-SHA quality gating, and prompt/rendering
    regressions.
- `cargo test --test listen listen_worker_times_out_builtin_turns_and_records_timeout_reporting -- --exact`
  - Passed.
  - Proved timeout reporting still records the distinct timeout contract instead
    of collapsing into stalled-turn handling.
- `cargo test --test listen listen_worker_logs_final_sigkill_termination_for_stubborn_builtin_timeouts -- --exact`
  - Passed.
  - Proved stubborn child processes still escalate from SIGTERM to SIGKILL with
    the configured grace window and log the final timeout termination outcome.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.
- `make quality`
  - Passed.
  - Covered `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`,
    full unit/integration test execution, and doc tests.

## Acceptance Criteria Mapping

- `tests/listen.rs` now reproduces the stale-overwrite shape and verifies the
  monotonic contract around blocked metadata, canonical metadata,
  `turn_history`, and `latest_resume_handle`.
- `src/listen/mod.rs` rejects stale daemon replacement when protected fields
  regress and preserves richer worker state across session reconciliation.
- `src/github_pr.rs` and `src/listen/worker.rs` resolve the active branch PR,
  current `headRefOid`, and `quality` workflow runs for that exact SHA before
  verification can report success.
- `src/listen/worker.rs` now blocks verification with remediation for missing,
  pending, failed, stale, or unresolvable workflow and PR metadata.
- Structured review prompt hardening is implemented in
  `src/listen/worker.rs` and anchored by the prompt probe exported through
  `src/lib.rs` plus listen integration coverage in `tests/listen.rs`.
- Reviewer-facing docs were updated in `README.md` and `WORKFLOW.md` to reflect
  the exact-SHA gate and its fail-closed behavior.

## Notes

- The first `make quality` pass surfaced one stale lib-test expectation in
  `src/listen/mod.rs`; updating that assertion to match the stronger
  stale-overwrite rejection semantics was the only follow-up required before the
  full gate passed cleanly.
- Timeout-path proofs were rerun explicitly after the continuation-turn timeout
  interruptions so review handoff does not rely on an inferred timeout state.
- The primary Linear issue description was not modified during this work. Ticket
  progress and validation status belong in the existing `## Codex Workpad`
  comment and the local validation artifacts above.
