# Validation: ENG-10996

Updated: 2026-04-14T20:16:09Z

## Scope

Locked the product-altitude vocabulary and Altitude 0 persona in repo-owned artifacts:

- `docs/product-altitude-workflow.md` is the canonical three-altitude reference for downstream work.
- `src/artifacts/workflows/product-altitude.md` adds a built-in workflow playbook for Altitude 0 story shaping.
- `src/workflows.rs` compiles the new built-in playbook into the workflow library.
- `README.md` and `src/artifacts/workflows/README.md` document the new built-in playbook.
- `tests/workflows.rs` covers the new list and explain surfaces.

## Command Proofs

| Command | Result | Evidence |
|---|---|---|
| `cargo fmt --check` | Passed | rustfmt reported no changes on the final tree. |
| `cargo test --test workflows` | Passed | 25 workflow tests passed on the final tree. |
| `./target/debug/intu agents workflows explain --root . product-altitude \| sed -n '1,60p'` | Passed | Output showed `Workflow: product-altitude`, `Source: builtin/product-altitude.md`, required parameters, validation rules, and the Altitude 0 persona. |
| `cargo clippy --all-targets --all-features -- -D warnings` | Passed | Finished successfully on the final tree. |
| `git diff --check` | Passed | No whitespace errors. |

## Linear Safety

- Reconciled and updated only the existing `## Codex Workpad` comment `a3e12946-2643-41b9-848f-8cc28f4b18cd`.
- Did not run any issue-description mutation command.
- Direct Linear read confirmed the `ENG-10996` issue description still contains the three-altitude description and does not contain the workpad body.

## Notes

- An initial `cargo test workflows` run failed because the new playbook had an unquoted YAML front matter item with a colon. The validation line was rewritten without the colon ambiguity, then `cargo test --test workflows` passed.
