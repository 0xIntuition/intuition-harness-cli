# ENG-10793 Validation Evidence

## Summary

Final remediation for `ENG-10793` restores repo-scoped listen instructions to
the compact first-turn instruction payload without re-expanding the full
workflow-contract dump. `src/listen/worker.rs` now emits a single path-based
instruction when `listen.instructions_path` is configured and a bounded fallback
notice when the configured file is missing from the workspace checkout.

Regression coverage now proves that first-turn instructions still render as the
same compact three-block contract, the configured repo-scoped listen
instructions path is surfaced to the agent on turn one, and the existing
instruction byte-budget guardrails remain intact. The end-to-end listen
bootstrap path also now asserts that launched agent instructions include the
repo-scoped listen instructions reference.

## Command Proofs

- `cargo test --lib listen::worker::tests::build_agent_instructions_surfaces_repo_scoped_listen_instructions_path -- --exact`
- `cargo test --lib listen::worker::tests::build_agent_instructions_renders_three_logical_blocks -- --exact`
- `cargo test --lib listen::worker::tests::first_turn_instruction_fixture_byte_budgets_hold_for_required_cases -- --exact`
- `cargo test listen_once_downloads_issue_attachment_context_for_agent --test listen -- --exact`
- `make quality`

## Results

- `cargo test --lib listen::worker::tests::build_agent_instructions_surfaces_repo_scoped_listen_instructions_path -- --exact`
  - Passed.
  - Proved turn-one instructions surface the configured repo-scoped listen
    instructions path in compact form.
- `cargo test --lib listen::worker::tests::build_agent_instructions_renders_three_logical_blocks -- --exact`
  - Passed.
  - Proved the first-turn instruction payload still renders exactly three
    logical blocks after the repo-scoped instructions hook returned.
- `cargo test --lib listen::worker::tests::first_turn_instruction_fixture_byte_budgets_hold_for_required_cases -- --exact`
  - Passed.
  - Proved the existing first-turn instruction budget fixtures still hold.
- `cargo test listen_once_downloads_issue_attachment_context_for_agent --test listen -- --exact`
  - Passed.
  - Proved the end-to-end listen bootstrap path exposes the configured
    repo-scoped listen instructions path to the launched agent while preserving
    the compact three-block structure.
- `make quality`
  - Passed cleanly on the final workspace tree after the targeted fix and final
    diff scrub.

## Acceptance Criteria Mapping

- `src/listen/worker.rs` now restores repo-scoped listen guidance to the
  compact first-turn instructions by pointing the agent at the configured file
  path instead of dumping the full workflow contract.
- `src/listen/worker.rs` keeps the compact three-block instruction layout and
  existing turn-one byte-budget guardrails intact.
- `tests/listen.rs` now asserts that first-turn instructions surface the
  configured repo-scoped listen instructions during the end-to-end listen
  bootstrap flow.

## Notes

- The repo-local branded packet path for ticket-specific validation evidence is
  `.intuition/backlog/ENG-10793/validation.md` in this repository.
- One unrelated listen integration fixture temporarily picked up the same
  `instructions_path` setup while patching the regression. That spillover was
  reverted before the final `make quality` rerun.
- The active Linear workpad target remains comment
  `4a090055-a3b6-4fc8-8f92-07557d6a5a0d`; it is updated in place during
  publication handoff rather than by mutating the primary issue description.

## Review Handoff

### Review Notes

#### Context Checkpoint

- Decisions made:
  - Restore repo-scoped listen guidance with a single path-based instruction
    instead of reintroducing the larger workflow-contract block.
  - Keep the three-block first-turn instruction structure unchanged.
  - Cover the regression at both the unit layer and the end-to-end listen
    bootstrap layer while preserving the existing fixture-backed budget checks.
- Failed approaches:
  - The earlier compaction removed the implicit workflow-contract hook, which
    dropped repo-scoped listen instructions from turn-one instructions entirely.
  - One unrelated listen integration test briefly inherited the same fixture
    setup during patching and had to be reverted before the final full-gate
    rerun.
- Blockers:
  - No local implementation or validation blocker remains.
- Exact remaining work:
  - Commit and push the final remediation.
  - Wait for GitHub Actions `quality` to finish on PR `#108` for the published
    head.
  - Rerun verification only if that remote gate reports a failure.
- Checklist and validation status:
  - Repo-scoped listen instructions are restored in the compact first-turn
    payload.
  - Targeted regression coverage for the fix passed.
  - `make quality` passed on the final workspace tree.
  - Reviewer-facing repo evidence is recorded in this file.
