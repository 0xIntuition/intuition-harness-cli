# ENG-10793 Validation Evidence

## Summary

Repo-level reviewer evidence for ENG-10793: listen turn-one artifacts are now
complementary instead of repetitive. Generated briefs drop the full
`## Linear Description` and placeholder-heavy `_Missing ..._` sections, keep
ticket identity and goal, point to repo-local codebase markdown paths instead
of inlining those files, and preserve verbatim inline `Validation`, `Test
Plan`, and `Acceptance Criteria` sections when present. Turn-one prompts now
render live execution metadata plus one compact description summary, prepend a
deterministic read-first attachment summary, keep discussion context as a
bounded newest-first summary, and avoid duplicating the same full issue body
across both the brief and the prompt. First-turn instructions now render as
exactly three logical blocks, while turn-two-and-beyond continuation prompts
remain unchanged.

The ticket also adds deterministic regression guardrails across the required
fixture classes: `small-happy-path`, `large-narrative`, `validation-heavy`,
`attachment-heavy`, and `discussion-heavy`. Those fixtures now back byte-budget
assertions for briefs, turn-one prompts, and first-turn instructions.

A final checkpoint follow-up also tightens discussion-summary budgeting so
reviewer labels count inside the total character cap instead of being excluded
from the budget math. A dedicated regression test now covers that edge case.

The final wrap-up also preserves nested markdown headings inside inline ticket
sections such as `Validation`, `Test Plan`, and `Acceptance Criteria`, while
still stripping those entire sections out of the turn-one narrative summary.

Because the previous listen turn timed out before the long-running repo gate
completed, this continuation reran `make quality` to a clean exit so review
handoff does not depend on partial validation output. A final live verification
pass then found that the tracked Linear workpad comment still held the stale
heuristic-review checkpoint body from `2026-04-10T04:05:01Z`, so this recovery
turn refreshed that comment in place and aligned the repo-level evidence to the
actual remote state.

## Command Proofs

- `cargo test --lib agents::tests::write_agent_brief_references_codebase_paths_without_linear_description -- --exact`
- `cargo test --lib agents::tests::write_agent_brief_preserves_verbatim_inline_ticket_sections -- --exact`
- `cargo test --lib listen::tests::render_agent_prompt_turn_one_avoids_full_description_duplication -- --exact`
- `cargo test --lib listen::tests::render_issue_attachment_manifest_adds_prioritized_summary -- --exact`
- `cargo test --lib listen::tests::render_agent_prompt_discussion_summary_is_newest_first_and_bounded -- --exact`
- `cargo test --lib listen::tests::render_discussion_summary_counts_labels_inside_total_budget -- --exact`
- `cargo test --lib listen::tests::extract_ticket_inline_sections_preserves_nested_headings -- --exact`
- `cargo test --lib listen::tests::render_issue_description_summary_strips_nested_inline_sections -- --exact`
- `cargo test --lib listen::worker::tests::build_agent_instructions_renders_three_logical_blocks -- --exact`
- `cargo test --lib listen::tests::render_continuation_prompt_is_compact -- --exact`
- `cargo test --lib agents::tests::write_agent_brief_fixture_byte_budgets_hold_for_required_cases -- --exact`
- `cargo test --lib listen::tests::turn_one_prompt_fixture_byte_budgets_hold_for_required_cases -- --exact`
- `cargo test --lib listen::worker::tests::first_turn_instruction_fixture_byte_budgets_hold_for_required_cases -- --exact`
- `cargo test listen_worker_codex_uses_continuation_prompt_on_resumed_turn --test listen -- --exact`
- `cargo test listen_worker_claude_uses_continuation_prompt_on_resumed_turn --test listen -- --exact`
- `cargo test --test listen -- --test-threads=1`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `make quality`

## Results

- `cargo test --lib agents::tests::write_agent_brief_references_codebase_paths_without_linear_description -- --exact`
  - Passed.
  - Proved generated briefs reference repo-local codebase paths and no longer
    render the full `## Linear Description` block.
- `cargo test --lib agents::tests::write_agent_brief_preserves_verbatim_inline_ticket_sections -- --exact`
  - Passed.
  - Proved inline ticket `Validation`, `Test Plan`, and `Acceptance Criteria`
    sections remain verbatim in the brief when present.
- `cargo test --lib listen::tests::render_agent_prompt_turn_one_avoids_full_description_duplication -- --exact`
  - Passed.
  - Proved turn-one prompt rendering avoids reusing the full issue description
    already preserved elsewhere in the brief flow.
- `cargo test --lib listen::tests::render_issue_attachment_manifest_adds_prioritized_summary -- --exact`
  - Passed.
  - Proved attachment context now starts with a deterministic read-first
    summary while keeping the full manifest below it.
- `cargo test --lib listen::tests::render_agent_prompt_discussion_summary_is_newest_first_and_bounded -- --exact`
  - Passed.
  - Proved discussion context is newest-first, capped to three bullets or
    excerpts, and bounded to the required character budget.
- `cargo test --lib listen::tests::render_discussion_summary_counts_labels_inside_total_budget -- --exact`
  - Passed.
  - Proved the total discussion-summary character budget now counts rendered
    labels as well as excerpt text, preventing over-budget bullets when labels
    are long.
- `cargo test --lib listen::tests::extract_ticket_inline_sections_preserves_nested_headings -- --exact`
  - Passed.
  - Proved nested markdown headings remain intact inside extracted inline
    ticket sections instead of being dropped during brief/prompt composition.
- `cargo test --lib listen::tests::render_issue_description_summary_strips_nested_inline_sections -- --exact`
  - Passed.
  - Proved turn-one narrative summaries still strip the full inline section,
    including any nested markdown subheadings under that section.
- `cargo test --lib listen::worker::tests::build_agent_instructions_renders_three_logical_blocks -- --exact`
  - Passed.
  - Proved first-turn instructions render as exactly three logical blocks while
    preserving workspace scoping, unattended execution posture, workpad sync,
    validation mirroring, and PR handoff rules.
- `cargo test --lib listen::tests::render_continuation_prompt_is_compact -- --exact`
  - Passed.
  - Proved continuation prompt output remains compact for turn two and beyond.
- `cargo test --lib agents::tests::write_agent_brief_fixture_byte_budgets_hold_for_required_cases -- --exact`
  - Passed.
  - Proved brief byte budgets across the five required fixture classes.
- `cargo test --lib listen::tests::turn_one_prompt_fixture_byte_budgets_hold_for_required_cases -- --exact`
  - Passed.
  - Proved turn-one prompt byte budgets across the same five required fixture
    classes.
- `cargo test --lib listen::worker::tests::first_turn_instruction_fixture_byte_budgets_hold_for_required_cases -- --exact`
  - Passed.
  - Proved first-turn instruction byte budgets across the same five required
    fixture classes.
- `cargo test listen_worker_codex_uses_continuation_prompt_on_resumed_turn --test listen -- --exact`
  - Passed.
  - Proved resumed Codex listen turns still use the continuation prompt path.
- `cargo test listen_worker_claude_uses_continuation_prompt_on_resumed_turn --test listen -- --exact`
  - Passed.
  - Proved resumed Claude listen turns still use the continuation prompt path.
- `cargo test --test listen -- --test-threads=1`
  - Passed.
  - Covered the full listen integration suite after the turn-one contract
    changes, including bootstrapping, attachment-context generation,
    continuation handling, and review-handoff flows.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.
- `make quality`
  - Passed cleanly.
  - Re-ran `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`,
    the full single-threaded test suite, and doc tests under the repository's
    primary quality gate.

## Acceptance Criteria Mapping

- `src/agents/brief.rs` and `src/agents/mod.rs` now keep the generated brief
  compact, path-based, and inline-section-preserving instead of description-
  duplicating or placeholder-heavy.
- `src/listen/mod.rs` now strips inline ticket sections from the narrative
  summary, renders a deterministic read-first attachment summary, and renders a
  bounded newest-first discussion summary while preserving the full manifest.
- `src/listen/worker.rs` now renders exactly three first-turn instruction
  blocks and still keeps resumed turns on the compact continuation-prompt path.
- `tests/fixtures/listen/turn-one/*`, `src/agents/mod.rs`, `src/listen/mod.rs`,
  `src/listen/worker.rs`, and `tests/listen.rs` add deterministic byte-budget
  and integration regression coverage across the five required fixture classes.
- `README.md` and `WORKFLOW.md` now document the slimmer complementary
  turn-one contract and unchanged continuation behavior.

## Notes

- The ticket-provided shorthand lib-test commands using `cargo test <name> --lib -- --exact`
  filtered to zero tests under Cargo. Validation used the exact
  module-qualified names recorded above.
- The rerun of `make quality` in this continuation turn specifically closes the
  remaining timeout risk from the prior listen turn, where the gate was still
  running when execution stopped.
- A final structured diff review against `origin/main` found no additional
  blocking issues in the landed turn-one compaction and regression-guardrail
  changes, so the only follow-up needed in this recovery turn was restoring
  remote checkpoint state.
- GitHub PR sweep for draft PR `#108` found no top-level comments beyond the
  Linear backlink, no review summaries, and no inline review comments.
- Final wrap-up also absorbed a late in-scope fix for nested inline markdown
  sections so brief extraction preserves subsection headings while turn-one
  narrative summaries continue to strip those sections cleanly.
- The current branch head already includes the nested-inline-heading follow-up,
  and local implementation changes were fully committed before this final
  checkpoint refresh. This turn only updated reviewer evidence and the live
  workpad to match the published branch state.
- An earlier interactive rerun first tripped two unrelated `managed_child`
  timeout tests and then a later PTY-backed rerun was terminated with
  `SIGTERM` late in the suite; isolated exact reruns for those
  `managed_child` tests passed immediately, and the final quiet rerun
  (`make quality >/tmp/eng10793-make-quality.log 2>&1`) completed cleanly on
  the same published branch head.
- `gh pr checks 108 --repo 0xIntuition/intuition-harness-cli` reported the
  matching GitHub Actions workflow named `quality` as pending at final
  checkpoint capture time.
- No Linear issue-description mutation command was run during validation, so
  the primary `ENG-10793` issue body remained unchanged.
- The active workpad target for this run is comment
  `4a090055-a3b6-4fc8-8f92-07557d6a5a0d`. A direct GraphQL read in this final
  recovery turn showed that comment still existed but still held the stale
  heuristic-review checkpoint body last updated at `2026-04-10T04:05:01Z`
  instead of the intended final handoff summary. The comment was then updated
  in place via the exported Linear API environment and re-read to verify that
  the live workpad matched the final checkpoint summary recorded here.

## Review Handoff

### Review Notes

#### Context Checkpoint

- Decisions made:
  - Slim turn-one artifacts across all three surfaces instead of only
    truncating one output: the brief, turn-one prompt, and first-turn
    instruction contract now each carry distinct information.
  - Keep inline ticket `Validation`, `Test Plan`, and `Acceptance Criteria`
    sections verbatim while replacing codebase markdown dumps with repo-local
    path references.
  - Add deterministic read-first attachment summaries, newest-first bounded
    discussion summaries, five fixture-backed byte-budget guards, and preserve
    byte-stable continuation prompt behavior for resumed turns.
  - Rerun `make quality` to completion in this continuation after the earlier
    listen turn timed out before the gate result was captured.
- Failed approaches:
  - The ticket's shorthand lib-test commands using `cargo test <name> --lib -- --exact`
    did not execute any tests under Cargo exact matching, so validation had to
    use the full module-qualified test names.
  - The previous listen turn stopped while the long-running repo gate was still
    in flight, so partial validation output was not sufficient for handoff.
  - The earlier checkpoint notes assumed the tracked Linear workpad comment had
    already been refreshed, but a direct remote read in this final turn showed
    the live comment still contained the stale heuristic-review checkpoint body
    rather than the real handoff summary.
- Blockers:
  - No local implementation or workpad-sync blocker remains.
  - The only remaining gate is the remote GitHub Actions `quality` run on
    draft PR `#108` before the shared review handoff can complete.
- Exact remaining work:
  - No additional local code or validation work remains in this workspace
    unless the remote `quality` run reports a failure.
  - Keep draft PR `#108` attached to the ticket, keep the reconciled workpad
    comment in place, and let the standard review handoff path consume the
    pushed branch once the remote workflow succeeds.
- Checklist and validation status:
  - Current branch head is pushed to `origin`.
  - Live Linear workpad comment `4a090055-a3b6-4fc8-8f92-07557d6a5a0d` was
    re-read, found to still contain the stale heuristic-review checkpoint body,
    and then updated in place to mirror this final checkpoint state.
  - Final structured review against `origin/main` found no new blocking issues.
  - Local implementation files were already committed before this checkpoint
    turn; only validation-note refreshes were edited afterward.
  - All ticket-required targeted tests passed using module-qualified exact test
    names where Cargo required them.
  - Fixture-backed regression budget coverage was added for the five required
    turn-one fixture classes.
  - `cargo test --test listen -- --test-threads=1` passed.
  - `cargo clippy --all-targets --all-features -- -D warnings` passed.
  - `make quality` passed.
  - Draft PR `#108` still has no actionable top-level comments, reviews, or
    inline review comments.
  - `gh pr checks 108 --repo 0xIntuition/intuition-harness-cli` reported the
    `quality` workflow as pending at checkpoint capture time.
  - Reviewer-facing evidence is recorded in this file.
