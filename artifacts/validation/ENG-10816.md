# ENG-10816 Validation Evidence

## Summary

`meta backlog improve` now uses the same scoped terminal-cleanup pattern as the
other shared TUI flows: callers explicitly enter the interactive or loading
terminal, keep a tiny drop guard in scope, and drop the active terminal/display
before printing shell-facing output. The refactor removes
`ImprovementTerminalSession` and stops `ImprovementLoadingDisplay` from owning
cleanup directly, while preserving `Ctrl+G` mouse toggling, non-TTY loading
behavior, and the existing backlog-improve review flow.

The regression coverage now proves the exact restore ordering:
cursor restore, then `DisableBracketedPaste`, `DisableMouseCapture`, and
`LeaveAlternateScreen`. It also adds a PTY-backed cancel-path proof showing that
the issue-picker escape path restores terminal state before printing
`Backlog improvement cancelled.`.

## Command Proofs

- `git fetch origin main`
- `git merge --ff-only origin/main`
- `cargo test improvement_terminal_cleanup_sequences_finish_before_summary_output -- --exact`
- `cargo test --lib backlog_improve::tests::improvement_terminal_cleanup_sequences_finish_before_summary_output -- --exact`
- `cargo test backlog_improve_interactive_cleanup_restores_terminal_state_before_return -- --exact`
- `cargo test backlog_improve_issue_picker_cancel_cleans_up_terminal_before_cancel_message -- --exact`
- `cargo test backlog_improve_scans_repo_backlog_and_writes_proposal_artifacts -- --exact`
- `cargo test --test backlog_improve -- --test-threads=1`
- `cargo fmt --check`
- `cargo clippy --all-targets --all-features -- -D warnings`

## Results

- `git fetch origin main`
  - Passed.
- `git merge --ff-only origin/main`
  - Passed.
  - Workspace was already current with `origin/main` at `663b3b8`.
- `cargo test improvement_terminal_cleanup_sequences_finish_before_summary_output -- --exact`
  - Completed, but Cargo filtered out the target because the unit regression's
    exact name is namespaced under `backlog_improve::tests::...`.
- `cargo test --lib backlog_improve::tests::improvement_terminal_cleanup_sequences_finish_before_summary_output -- --exact`
  - Passed.
  - Executed the tightened unit regression that now locks the exact cleanup
    command ordering before summary output.
- `cargo test backlog_improve_interactive_cleanup_restores_terminal_state_before_return -- --exact`
  - Passed.
  - Verified the hidden cleanup-byte seam still proves normal completion leaves
    no terminal escape bytes in the final summary tail.
- `cargo test backlog_improve_issue_picker_cancel_cleans_up_terminal_before_cancel_message -- --exact`
  - Passed.
  - Verified the explicit cancel path cleans up the interactive terminal before
    the shell-facing cancel line is written.
- `cargo test backlog_improve_scans_repo_backlog_and_writes_proposal_artifacts -- --exact`
  - Passed.
  - Confirmed the repo-scoped scan flow and proposal artifact writes still work
    after the terminal lifecycle refactor.
- `cargo test --test backlog_improve -- --test-threads=1`
  - Passed.
  - Full `tests/backlog_improve.rs` suite passed with all 9 regressions green.
- `cargo fmt --check`
  - Passed.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.

## Acceptance Criteria Mapping

- [`src/backlog_improve.rs`](../../src/backlog_improve.rs) now enters interactive
  and loading terminals explicitly and relies on scoped cleanup guards instead
  of `ImprovementTerminalSession` / `ImprovementLoadingDisplay` lifecycle
  ownership.
- [`src/backlog_improve.rs`](../../src/backlog_improve.rs) still restores the
  cursor before disabling bracketed paste, mouse capture, and the alternate
  screen, and still delays briefly after interactive cleanup so the parent shell
  redraw is stable.
- [`tests/backlog_improve.rs`](../../tests/backlog_improve.rs) proves normal
  completion output and the explicit issue-picker cancel path both finish
  cleanup before shell-facing text is emitted.
- [`src/lib.rs`](../../src/lib.rs) continues to expose the hidden
  `backlog_improve_terminal_cleanup_bytes()` seam so the crate-external
  regression can keep asserting deterministic cleanup bytes.

## Notes

- Live Linear reads and workpad sync were attempted with the injected
  `LINEAR_API_KEY`, but the live API returned `AUTHENTICATION_ERROR`, so the
  run recorded reviewer-facing evidence locally instead of mutating the remote
  workpad comment.
- No live Linear mutation command was run from this workspace, so the primary
  Linear issue description was not overwritten during validation.
- Draft PR `#107` exists for this branch with the `metastack` label. The PR
  feedback sweep found only the Linear linkback top-level comment, no review
  summaries, and no inline review comments.
- At checkpoint time the exact-head GitHub Actions `quality` run for PR `#107`
  was still in progress, so the branch is code-complete locally but not yet
  ready for a ready-promotion handoff from this session.

## Review Notes

### Context Checkpoint

- Decisions made:
  - keep the hidden cleanup-byte seam so the crate-external regression can
    continue asserting deterministic cleanup ordering
  - replace `ImprovementTerminalSession` / loading-owned cleanup with explicit
    terminal setup plus scoped guards
  - drop the active terminal or loading display before printing shell-facing
    summary or cancel output
  - leave source code unchanged in this final turn because the implementation
    and local validation were already complete; use the local packet and this
    artifact as the durable fallback checkpoint instead
- Failed approaches:
  - the ticket-listed bare exact unit-test command completed but matched zero
    tests, so the lib-qualified exact path was used to execute the actual unit
    regression body
  - live Linear workpad reads and updates remained blocked because the injected
    auth path returned `AUTHENTICATION_ERROR`, and the installed Linear
    connector still returned `Entity not found` for the ticket by identifier
    and UUID
- Blockers:
  - remote Linear workpad sync is still blocked because issue `ENG-10816` /
    `6a1a9db5-7860-4da4-b9e6-d99f42cc6932` and comment
    `b3ab968f-a3ab-4599-859c-27d368c12eea` could not be resolved from the
    available auth/profile context
  - no local code or repo-validation blocker remains
- Exact remaining work:
  - reconcile the remote `## Codex Workpad` comment once a Linear auth/profile
    path can resolve the current issue and comment ids
  - recheck the exact-head GitHub `quality` workflow for PR `#107` after any
    later checkpoint-only push before promoting the draft PR
- Checklist and validation status:
  - all targeted backlog-improve regressions passed, including the new
    cancel-path cleanup proof
  - `cargo fmt --check` passed
  - `cargo clippy --all-targets --all-features -- -D warnings` passed
  - PR `#107` currently has no actionable top-level, inline, or review-summary
    feedback
