# ENG-10744 Validation Evidence

## Summary

Repo-level reviewer evidence for ENG-10744: listen reconciliation now classifies dead stored worker
PIDs, automatically relaunches retryable listen-origin running sessions from persisted context with
a fixed `2`-attempt budget, and preserves additive stale-worker recovery metadata across session
JSON, mirrored detail JSON, inspect output, and dashboard selected-session detail rendering.

## Command Proofs

- `cargo test --test listen -- --test-threads=1`
- `cargo test listen::store --lib`
- `cargo test listen::dashboard --lib`
- `cargo test listen::mod --lib`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `make quality`

## Results

- `cargo test --test listen -- --test-threads=1`
  - Passed with `94 passed; 0 failed`.
  - Exercised the full listen integration matrix, including inspect output for stale-worker
    recovery metadata.
- `cargo test listen::store --lib`
  - Passed with `34 passed; 0 failed`.
  - Proved legacy session/detail compatibility and manual retry reset behavior for stale-worker
    recovery metadata.
- `cargo test listen::dashboard --lib`
  - Passed with `31 passed; 0 failed`.
  - Proved selected-session detail rendering includes stale-worker recovery metadata.
- `cargo test listen::mod --lib`
  - Passed with `0 matched; 0 failed` for that exact filter.
  - The filter is empty in this crate, so focused stale-worker reconciliation behavior is covered
    by the broader lib and integration runs instead.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.
- `make quality`
  - Passed.
  - Covered `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and
    the full `cargo test -- --test-threads=1` suite.

## Acceptance Criteria Mapping

- Additive stale-worker fields and operator summary helpers are implemented in
  `src/listen/state.rs`.
- Backward-compatible load, repair, detail mirroring, and manual retry reset behavior are
  implemented in `src/listen/store.rs` and covered by:
  - `load_state_backfills_stale_worker_fields_from_old_payloads`
  - `retry_blocked_session_resets_stale_worker_recovery_window`
- Centralized dead-worker classification, bounded recovery, and terminal blocking are implemented in
  `src/listen/mod.rs` and covered by:
  - `stale_running_session_relaunches_with_existing_context_and_fresh_pid`
  - `stale_running_session_budget_exhaustion_blocks_without_relaunch`
- Worker rewrite preservation of stale-worker metadata is implemented in `src/listen/worker.rs`.
- Inspect output coverage is provided by
  `tests/listen.rs::listen_sessions_inspect_renders_stale_worker_recovery_metadata`.
- Dashboard detail coverage is provided by
  `src/listen/dashboard.rs::tests::session_detail_renders_stale_worker_recovery_metadata`.
- Reviewer/operator docs were updated in `README.md`, `docs/agent-daemon.md`, and
  `docs/listen-phased-execution-spec.md`.

## Notes

- The automatic stale-worker recovery budget is intentionally fixed at `2` attempts and does not
  add a new config surface in this slice.
- Recovery metadata now survives worker rewrites and is mirrored through the existing
  `session-details/<ISSUE>.json` contract instead of introducing a parallel store.
- The continuation-turn timeout note was operational rather than a stuck workspace: before final
  handoff there were no orphaned `cargo` or `make quality` processes, the workspace had `104 GiB`
  free, and the final-tree `make quality` rerun passed cleanly.
- The primary Linear issue description was not modified during this work.
