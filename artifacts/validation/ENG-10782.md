# ENG-10782 Validation Evidence

## Summary

Repo-level reviewer evidence for ENG-10782: listen now resolves one
configurable context budget across CLI, repo, install, and built-in defaults;
workers derive context pressure from completed-turn known input tokens only;
high and critical pressure trigger durable checkpoint behavior against the
effective workpad body; and the selected-session detail pane now renders live
context pressure using the same resolved context-budget contract as the worker
path, without persisting `ContextPressure` itself or adding a new session-table
column.

The final validation pass also repaired the continuation-turn regression that
surfaced during this run: after adding the `Context Pressure` detail field, the
execute-origin operator note fell below the render-once fold in the dashboard
detail pane. `src/listen/dashboard.rs` now renders that execute-origin note
immediately after the summary fields so the render-once contract remains
visible.

The verification-repair follow-up then fixed the remaining non-default-budget
gap: session-detail hydration now resolves the effective context budget from the
session workspace and install defaults before the dashboard derives pressure,
and dedicated regressions prove both repo-budget resolution and overridden
budget rendering.

## Command Proofs

- `cargo run -- agents listen --help | rg "context-budget-tokens"`
- `cargo run -- listen sessions resume --help | rg "context-budget-tokens"`
- `cargo run -- runtime config --help | rg "listen-context-budget-tokens"`
- `cargo run -- runtime setup --help | rg "listen-context-budget-tokens"`
- `cargo test --lib effective_listen_context_budget_tokens_follow_repo_install_default_precedence`
- `cargo test --lib context_pressure`
- `cargo test --lib checkpoint`
- `cargo test --lib selected_session_detail_uses_session_turn_history_for_context_pressure`
- `cargo test --lib load_session_details_resolves_repo_context_budget_for_workspace`
- `cargo test --lib selected_session_detail_uses_overridden_context_budget_for_context_pressure`
- `cargo test --test listen listen_render_once_demo_detail_shows_execute_origin_for_execute_session`
- `cargo fmt --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test`
- `make quality`

## Results

- `cargo run -- agents listen --help | rg "context-budget-tokens"`
  - Passed.
  - Help output includes `--context-budget-tokens <CONTEXT_BUDGET_TOKENS>`.
- `cargo run -- listen sessions resume --help | rg "context-budget-tokens"`
  - Passed.
  - Help output includes `--context-budget-tokens <CONTEXT_BUDGET_TOKENS>`.
- `cargo run -- runtime config --help | rg "listen-context-budget-tokens"`
  - Passed.
  - Help output includes
    `--listen-context-budget-tokens <LISTEN_CONTEXT_BUDGET_TOKENS>`.
- `cargo run -- runtime setup --help | rg "listen-context-budget-tokens"`
  - Passed.
  - Help output includes
    `--listen-context-budget-tokens <LISTEN_CONTEXT_BUDGET_TOKENS>`.
- `cargo test --lib effective_listen_context_budget_tokens_follow_repo_install_default_precedence`
  - Passed.
  - Proved repo defaults override install defaults, CLI overrides win over both,
    and the built-in `180000` fallback remains intact.
- `cargo test --lib context_pressure`
  - Passed.
  - Covered the pressure thresholds and confirmed only completed-turn known
    input tokens contribute to the derived pressure band.
- `cargo test --lib checkpoint`
  - Passed.
  - Covered effective-workpad checkpoint detection and preservation behavior.
- `cargo test --lib selected_session_detail_uses_session_turn_history_for_context_pressure`
  - Passed.
  - Proved the detail pane derives pressure from the selected session
    `turn_history` rather than from a new persisted field.
- `cargo test --lib load_session_details_resolves_repo_context_budget_for_workspace`
  - Passed.
  - Proved selected-session detail hydration resolves the repo-scoped override
    from the workspace metadata before the dashboard renders context pressure.
- `cargo test --lib selected_session_detail_uses_overridden_context_budget_for_context_pressure`
  - Passed.
  - Proved a session detail rendered under a non-default `100000` token budget
    produces the expected higher pressure label instead of silently using the
    built-in default budget.
- `cargo test --test listen listen_render_once_demo_detail_shows_execute_origin_for_execute_session`
  - Passed after the final dashboard-ordering fix.
  - Proved the render-once detail view still keeps the execute-origin operator
    note visible alongside the new `Context Pressure` summary field.
- `cargo fmt --check`
  - Passed.
- `cargo clippy --all-targets --all-features -- -D warnings`
  - Passed cleanly.
- `cargo test`
  - Passed cleanly after the render-once regression fix.
  - Covered the full unit, integration, and doc-test matrix, including the
    `tests/listen.rs` integration suite where the regression surfaced.
- `make quality`
  - Passed cleanly after the verification repair work.
  - Re-ran `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`,
    the full single-threaded test suite, and doc tests under the repo’s primary
    quality gate.

## Acceptance Criteria Mapping

- `src/config.rs`, `src/config_command.rs`, `src/setup.rs`, `src/cli.rs`, and
  `src/listen/mod.rs` implement the requested context-budget precedence and
  propagate the resolved numeric budget into every hidden worker launch path.
- `src/listen/state.rs` derives `ContextPressure` from completed-turn known
  input tokens only and keeps the pressure state computed rather than persisted.
- `src/listen/workpad.rs` and `src/listen/worker.rs` use the effective workpad
  body (`pending_linear_sync.workpad_body` first, active Linear workpad second)
  for checkpoint detection, checkpoint preservation, and later review-workpad
  rewrites.
- `src/listen/worker.rs` limits only remaining execution turns under `critical`
  pressure, preserves one-time checkpointing, and keeps timeout handling
  distinct from repeated no-progress stall handling.
- `src/listen/store.rs` and `src/listen/dashboard.rs` now hydrate and consume
  the selected session’s effective context budget before deriving detail-pane
  pressure, while still using the selected session `turn_history` and keeping
  the session table unchanged.
- `src/listen/dashboard.rs` also keeps the execute-origin render-once operator
  note visible after the new field addition.
- `README.md`, `WORKFLOW.md`, `docs/agent-daemon.md`, and
  `docs/listen-phased-execution-spec.md` document the new context-budget and
  pressure/checkpoint behavior in the same change.

## Notes

- The first full `cargo test` run found one real regression instead of an
  infrastructure-only timeout: `tests/listen.rs::listen_render_once_demo_detail_shows_execute_origin_for_execute_session`
  failed because the new summary field displaced an existing operator-facing
  sentence below the visible fold.
- The corrective changes stayed narrow: move the execute-origin note higher in
  the selected-session detail rendering, then hydrate the effective
  context-budget value onto the selected-session detail before deriving
  pressure.
- The local data volume still reported only about `6.3Gi` free during the
  follow-up validation pass, but `make quality` completed successfully anyway.
- No Linear mutation command was run from this validation pass, so the primary
  Linear issue description remained unchanged.
